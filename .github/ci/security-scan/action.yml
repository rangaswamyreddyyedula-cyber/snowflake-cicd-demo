name: Security scan
description: Checkov scan of the Terraform code. Reports findings without failing the build.

runs:
  using: composite
  steps:
    - name: Checkov
      uses: bridgecrewio/checkov-action@v12
      with:
        directory: terraform
        framework: terraform
        soft_fail: true
